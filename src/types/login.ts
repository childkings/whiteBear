export namespace loginVue {
  export type loginVueState = 'login' | 'register'
  export interface radiusMove {
    left?: number
    top?: number
  }
  export interface loginMove {
    right?: number
    bottom?: number
  }
}